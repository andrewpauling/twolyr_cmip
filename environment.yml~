name: twolyr_cmip
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - jupyter
  - jupyterlab
  - xarray
  - statsmodels
  - cftime
  - numpy
  - scipy
  - dask
  - netcdf4
  - pandas
  - matplotlib
